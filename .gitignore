/vendor/
/src-old
composer.lock
/.phpunit.cache/
.phpunit.result.cache

# Test residue: the fixture app is a real base_path, so logging during tests lands here.
tests/Fixtures/app/storage/

# Local workspace orientation — machine-specific, must not ship with the package.
CLAUDE.local.md
